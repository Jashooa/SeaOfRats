#pragma once

#include "include/SDK/SDK.h"

namespace Hacks
{
    namespace ESP
    {
        class Barrel
        {
        public:
            static void Draw(SDK::UWorld* world, SDK::AActor* actor);
        };
    }
}
