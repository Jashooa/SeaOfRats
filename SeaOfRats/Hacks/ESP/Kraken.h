#pragma once

#include "include/SDK/SDK.h"

namespace Hacks
{
    namespace ESP
    {
        void DrawKraken(SDK::UWorld* world, SDK::AActor* actor);
        void DrawKrakenTentacle(SDK::UWorld* world, SDK::AActor* actor);
    }
}
